'use client';

import { observer } from 'mobx-react-lite';

type Props = {

};
export const Reports = observer(function(props: Props) {
  const { } = props;
  return (
    <div className="w-full bg-white max-w-full overflow-y-auto p-10">
      Reports Loading...
    </div>
  );
});
